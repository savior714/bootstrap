[project]
name = "agent-harness-bootstrap"
version = "0.0.0"
description = "Repository tooling for the versioned agent harness Copier template"
requires-python = ">=3.11"
dependencies = [
  "copier==9.17.0",
  "pyyaml>=6.0,<7",
]

[tool.uv]
package = false
