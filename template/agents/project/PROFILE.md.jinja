# Project Profile — {{ project_name }}

이 파일은 **project-owned overlay**다.
초기 생성 후 프로젝트가 직접 관리하며 Copier update가 기존 내용을 덮어쓰지 않는다.

## Identity

- Project name: `{{ project_name }}`
- Project slug: `{{ project_slug }}`
- Canonical branch: `{{ canonical_branch }}`
- Package/build tool: `{{ package_tool }}`

## Commands

- Lint: `{{ lint_command or 'NOT_CONFIGURED' }}`
- Typecheck: `{{ typecheck_command or 'NOT_CONFIGURED' }}`
- Targeted test: `{{ targeted_test_command or 'NOT_CONFIGURED' }}`
- Release check: `{{ release_check_command or 'NOT_CONFIGURED' }}`

`NOT_CONFIGURED` command를 추측해서 실행하지 않는다.
실제 저장소 config를 확인한 뒤 이 파일을 갱신한다.

## Capabilities

- Runtime visual: `{{ has_runtime_visual }}`
- Database: `{{ has_database }}`
- Content provenance: `{{ has_content_provenance }}`
- Regulated domain: `{{ regulated_domain }}`

## Project-specific additions

여기에 다음을 기록한다.

- 실제 디렉터리와 subsystem 경계
- 인증·DB·배포 구조
- 고위험 도메인
- 추가 targeted validation
- 프로젝트에서만 적용되는 금지사항
