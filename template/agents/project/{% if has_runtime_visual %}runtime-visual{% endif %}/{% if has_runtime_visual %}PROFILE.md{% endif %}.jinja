# {{ project_name }} Runtime Visual Profile

이 파일은 **project-owned overlay**다.
초기 생성 후 프로젝트가 직접 관리하며 Copier update 가 기존 내용을 덮어쓰지 않는다.

PROFILE_STATUS: INCOMPLETE

## Runtime target
- Target base URL: `<required>`
- Target hostname/port: `<required>`

## Deployment identity
- Deployment identity command: `<required>`
- Expected identity format: `<required>`

## Browser policy
- Stable browser/channel: `<required>`
- Headless regression command: `<required>`
- Headed visual command: `<required>`
- Browser binary path (if non-default): `<none-or-required>`

## Authentication
- Existing auth state path: `<none-or-required>`
- Auth state refresh policy: `<required-when-applicable>`
- External IdP blocker code: `<required-when-applicable>`

## Viewports
- Required viewports: `<required>`
- Device emulation settings: `<none-or-required>`

## Exact commands
- Full acceptance command: `<required>`
- Individual test execution command: `<none-or-required>`

## Project-specific blocker codes
- Auth failure codes: `<required-when-applicable>`
- Target unavailable codes: `<required-when-applicable>`
- Other runtime blockers: `<required-when-applicable>`

## Completion checklist

Before marking PROFILE_STATUS as READY:

- [ ] All `<required>` fields filled with actual values
- [ ] Browser/channel verified available in target environment
- [ ] Authentication policy confirmed with team
- [ ] Acceptance command tested manually
- [ ] Blocker codes documented from actual runtime experience

Once checklist is complete, change:

```markdown
PROFILE_STATUS: READY
```

Core treats `INCOMPLETE` as non-executable.
